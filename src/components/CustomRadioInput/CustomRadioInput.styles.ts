import { mergeStyles } from "@fluentui/react";

export const inputContainerStyles = mergeStyles({
  fontSize: 20,
  marginLeft: "5%",
  gap: 15,
  display: "flex",
  marginBottom: 30,
  alignItems: "center",
});

export const labelHeadingStyles = mergeStyles({
  fontWeight: 500,
  fontSize: 22,
});
